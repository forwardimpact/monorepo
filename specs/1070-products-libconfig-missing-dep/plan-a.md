# Plan 1070-a — Declare `@forwardimpact/libconfig` as a dependency in four products

Per dickolsson's approval on PR #984: skip the design, write a short plan
for completeness, implement directly. This plan is intentionally terse —
the spec carries the *why* and the *what*; this carries the *how* and the
*sequencing*.

## Approach

1. Add `@forwardimpact/libconfig` to the `dependencies` of map, landmark,
   summit, and guide. Range expression: `^0.1.79`. `semver.minVersion`
   resolves to `0.1.79` for all four — the spec's only correctness
   constraint on the range string.
2. Add a contributor-side guard, `scripts/check-workspace-imports.mjs`,
   that scans `products/*` source files for `@forwardimpact/*` imports
   (static, dynamic, and re-export forms) and fails when the importing
   package's `package.json` does not declare the imported package in any
   of `dependencies`, `devDependencies`, `peerDependencies`, or
   `optionalDependencies`.
3. Wire the guard into the existing `bun run context` chain (the same
   chain that runs on every PR via the `Data` workflow). Exposed as
   `bun run context:check-workspace-imports`.
4. Add three tests under `tests/`: a manifest assertion using
   `semver.minVersion`, a unit test for the guard's check function, and a
   doc assertion against `products/CLAUDE.md`.
5. Add a "Workspace dependencies" section to `products/CLAUDE.md` that
   references the guard by name (`check-workspace-imports`).

## File changes

| File | Change |
|---|---|
| `products/map/package.json` | Add `"@forwardimpact/libconfig": "^0.1.79"` in `dependencies` |
| `products/landmark/package.json` | Add `"@forwardimpact/libconfig": "^0.1.79"` in `dependencies` |
| `products/summit/package.json` | Add `"@forwardimpact/libconfig": "^0.1.79"` in `dependencies` |
| `products/guide/package.json` | Bump `"@forwardimpact/libconfig"` from `^0.1.58` to `^0.1.79` in `dependencies` |
| `package.json` (root) | Add `semver` to root `devDependencies` (used by the spec-1070 manifest test); add `context:check-workspace-imports` script; chain it into `context` |
| `scripts/check-workspace-imports.mjs` | New file — the guard. Exports `findUndeclaredImports({ files, manifests })` for the unit test; runs as a CLI when invoked directly, scoped to `products/*`, walks each product's source files, parses with `acorn` (already a root devDep), reports findings, exits non-zero on any |
| `tests/spec-1070-libconfig-deps.test.js` | New test — reads each of the four product `package.json` files, asserts `dependencies["@forwardimpact/libconfig"]` is present and `semver.minVersion(range) === "0.1.79"` |
| `tests/spec-1070-workspace-imports-guard.test.js` | New test — calls `findUndeclaredImports` with synthetic in-memory file content and manifest objects; asserts a static import of an undeclared workspace package produces a finding identifying both file path and missing package; asserts a static import of a declared workspace package produces no finding |
| `tests/spec-1070-products-claude-doc.test.js` | New test — reads `products/CLAUDE.md`, asserts presence of (a) a sentence requiring `@forwardimpact/*` imports inside published products to appear in that product's `package.json`, and (b) a reference to `check-workspace-imports` |
| `products/CLAUDE.md` | Add a "Workspace dependencies" section recording the rule and naming the guard |
| `bun.lock` | Regenerated by `bun install` after the manifest edits |

## Guard design

- **Scope** — `products/*/{bin,src,test}/**/*.js`. Skips
  `products/*/node_modules/**`, `products/*/dist/**`. Libraries and services are
  out of scope (deferred to a follow-up spec — see spec 1070 § Out of scope).
- **Import detection** — Static `import`/`export` declarations via `acorn` AST
  walk (consistent with `tests/no-legacy-handler-shape.test.js`). Also catches
  `await import("…")` (dynamic, `CallExpression` with callee `Import`). JSDoc
  `import("…")` type references are *not* detected — they don't fail at runtime,
  and the spec accepts either choice.
- **Manifest lookup** — Walks upward from each source file to the nearest
  `package.json`; collects keys from `dependencies`, `devDependencies`,
  `peerDependencies`, `optionalDependencies` into a set; any `@forwardimpact/*`
  import not in that set is a finding.
- **Output** — One line per finding:
  `products/<pkg>/<path>:<line>: imports "@forwardimpact/<pkg>" but it is not declared in products/<pkg>/package.json`.
  Exits 1 on any finding, 0 otherwise.
- **Exports** — `findUndeclaredImports({ files, manifests })` for the unit test,
  where each entry of `files` is `{ path, source }` and `manifests` is
  `{ [packageDir]: { dependencies, devDependencies, ... } }`. The CLI builds
  these inputs from disk; the test passes them directly.

## Verification

On the implementation branch:

1. `bun run context:check-workspace-imports` exits 0.
2. `bun run context` exits 0 (the guard is in the chain).
3. `bun run test` exits 0 (the three new tests pass, and existing tests stay
   green).
4. `bun run check` exits 0 (format/lint/jsdoc/harness, plus context above).
5. `bun install` produces a clean diff for `bun.lock`.

The plan does not commit to whether release-cut auto-bumps the four
products from these `package.json`-only edits. The implementation branch
ships green regardless; release-cut behaviour is verified post-merge.

## Pre-flight risk

The guard fires on `products/*`. If any product *other than* the four
named ones has an undeclared `@forwardimpact/*` import, the guard will
fail on the implementation branch. The implementation step orders
"run the guard against `products/*`" before "wire it into CI" — any
surprise violation gets fixed (declaring the missing dep) or surfaced as
a scope question to the user before the branch goes for review.

## Out of scope

- Libraries and services — deferred to a follow-up spec.
- The publish-side guard that would catch export gaps in the published
  tarball — deferred.
- Inter-package publish-race hardening — deferred.

— Staff Engineer 🛠️
