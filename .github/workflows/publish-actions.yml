name: "Publish: Actions"

# Outbound mirror for the six co-located composite actions — the four agent-run
# actions under products/gemba/actions/ and the two Kata actions under
# products/kata/actions/. Each leg splits its monorepo prefix with the pinned
# splitsh-lite and non-force-pushes the projection to its sibling repo's main
# (see ./.github/actions/split-and-push). Add or retire an action by editing
# one matrix entry.
#
# Fires on every push to main that touches an action source (paths-filtered),
# plus manual dispatch. The lineage was seeded once (the only sanctioned force);
# every run here is a non-force fast-forward, and a non-fast-forward — a sibling
# main that diverged from the projection — is rejected and fails the run.
on:
  push:
    branches: [main]
    paths:
      - "products/gemba/actions/harness/**"
      - "products/gemba/actions/benchmark/**"
      - "products/gemba/actions/wiki/**"
      - "products/gemba/actions/bootstrap/**"
      - "products/kata/actions/kata-agent/**"
      - "products/kata/actions/kata-interview/**"
      - ".github/actions/split-and-push/**"
      - ".github/workflows/publish-actions.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    name: ${{ matrix.action.repo }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        action:
          - prefix: products/gemba/actions/harness
            repo: harness
          - prefix: products/gemba/actions/benchmark
            repo: benchmark
          - prefix: products/gemba/actions/wiki
            repo: wiki
          - prefix: products/kata/actions/kata-agent
            repo: kata-agent
          - prefix: products/kata/actions/kata-interview
            repo: kata-interview
          - prefix: products/gemba/actions/bootstrap
            repo: bootstrap
    steps:
      - name: Generate installation token
        id: ci-app
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          app-id: ${{ secrets.KATA_APP_ID }}
          private-key: ${{ secrets.KATA_APP_PRIVATE_KEY }}
          repositories: ${{ matrix.action.repo }}

      # Check out the monorepo (full history — the split needs it). The split
      # and the local-action references below resolve against this checkout,
      # never a sibling.
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0
          # Do not leave the default-token http.extraheader in git config; it
          # would override the App token in the cross-repo push URL and the push
          # would be denied as github-actions[bot].
          persist-credentials: false

      - uses: ./.github/actions/audit
        with:
          vulnerability-scanning: "false"
          secret-scanning: "true"

      - name: Split and push
        uses: ./.github/actions/split-and-push
        with:
          prefix: ${{ matrix.action.prefix }}
          sibling-repo: ${{ matrix.action.repo }}
          token: ${{ steps.ci-app.outputs.token }}
