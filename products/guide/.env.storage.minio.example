# ==========================================
# MinIO Storage Configuration
# ==========================================
# Use MinIO for S3-compatible object storage.
# Copy to .env.storage.minio and fill in passwords.
#
# Endpoint configuration depends on your setup:
#   - Local Node.js + Docker MinIO: http://localhost:9000
#   - Docker services + Docker MinIO: http://storage-minio.guide.local:9000
# ==========================================

# ==========================================
# Storage Backend
# ==========================================

STORAGE_TYPE=s3
S3_REGION=us-east-1
S3_BUCKET_NAME=guide

# ==========================================
# Storage S3 Protocol Configuration
# ==========================================
# Used by services to access MinIO via S3 protocol.
#
# For local environment:	http://localhost:9000
# For Docker environment:	http://storage-minio.guide.local:9000

AWS_ENDPOINT_URL=http://localhost:9000
# AWS_ACCESS_KEY_ID=your_s3_access_key
# AWS_SECRET_ACCESS_KEY=your_s3_secret_key
