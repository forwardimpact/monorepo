# ==========================================
# Docker Compose Configuration
# ==========================================
# Proxy and networking for Docker containers.
# Copy to .env.docker when using docker compose.
# ==========================================

# ==========================================
# Proxy Configuration
# ==========================================
# Required for containers to access external APIs through the gateway.

HTTPS_PROXY=http://gateway.guide.local:3128
HTTP_PROXY=http://gateway.guide.local:3128
NO_PROXY=localhost,127.0.0.1,*.guide.local

# ==========================================
# Embedding Service (TEI)
# ==========================================

EMBEDDING_BASE_URL=http://tei.guide.local:8080
