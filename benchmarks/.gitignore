# Artifacts every family generates at run time (`apm install` / `fit-benchmark
# run`). Ignored once here so families don't each repeat them. `apm_modules/`
# and `node_modules/` are already covered by the repo-root .gitignore.
*/.claude/
*/apm.lock.yaml
