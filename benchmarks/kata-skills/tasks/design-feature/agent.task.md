The spec for this feature is approved. You find it at
`specs/042-todo-filter/spec.md`. Read it. Then study the mock app under `app/`
so the design reflects the real components.

Follow the `kata-design` skill (staged under `.claude/skills/kata-design/`).
Write an architectural design at `specs/042-todo-filter/design-a.md`.

Capture WHICH and WHERE: the components and interfaces involved, the data flow,
and the key decisions. Each decision must name at least one rejected
alternative and say why. Stay within the spec's scope. Keep the design under
200 lines. Write the design only. Add no implementation plan and no code
changes.
