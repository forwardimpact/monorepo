The spec for this feature is approved and provided at
`specs/042-todo-filter/spec.md`. Read it, then study the mock app under `app/`
so the design reflects the real components.

Following the `kata-design` skill (staged under `.claude/skills/kata-design/`),
write an architectural design at `specs/042-todo-filter/design-a.md`.

Capture WHICH and WHERE: the components and interfaces involved, the data flow,
and the key decisions — each decision naming at least one rejected alternative
and why. Stay within the spec's scope. Keep the design under 200 lines. Write
the design only — no implementation plan and no code changes.
