The spec, design, and plan for this feature are approved and provided in
`specs/042-todo-filter/` (`spec.md`, `design-a.md`, `plan-a.md`). The mock app
to change is under `app/`.

Following the `kata-implement` skill (staged under
`.claude/skills/kata-implement/`), implement `plan-a.md` step by step against
the app under `app/`. Read the spec for WHAT/WHY, the design for WHICH/WHERE,
and the plan for HOW/WHEN before editing.

Implement only what the plan describes — no unrequested refactors, features, or
cleanup. Verify your work with the app's own test command (`node --test` from
within `app/`) as you go. Do not edit anything under `specs/`.
