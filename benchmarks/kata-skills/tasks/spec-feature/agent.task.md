Read `brief.md` and `jtbd-excerpt.md`, and study the mock app under `app/` so
the spec is grounded in how the CLI and store actually behave.

Following the `kata-spec` skill (staged under `.claude/skills/kata-spec/`),
write a specification at `specs/042-todo-filter/spec.md` that addresses the
brief. Quote the JTBD persona+job verbatim using the exact `<persona>: <job>`
string from the second-level heading of `jtbd-excerpt.md`.

Write the spec only — no design, no plan. Capture WHAT and WHY, not HOW: do not
name files or include `file:line` references.
