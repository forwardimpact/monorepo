Read `brief.md` and `jtbd-excerpt.md`. Then study the mock app under `app/`.
This grounds the spec in how the CLI and store actually behave.

Follow the `kata-spec` skill (staged under `.claude/skills/kata-spec/`). Write a
specification at `specs/042-todo-filter/spec.md` that addresses the brief. Quote
the JTBD persona+job verbatim. Use the exact `<persona>: <job>` string from the
second-level heading of `jtbd-excerpt.md`.

Write the spec only. Add no design and no plan. Capture WHAT and WHY. Do not
capture HOW. Do not name files. Do not include `file:line` references.
