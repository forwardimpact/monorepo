The spec and design for this feature are approved. You find them at
`specs/042-todo-filter/spec.md` and `specs/042-todo-filter/design-a.md`. Read
both. Then study the mock app under `app/` so the plan targets the real files.

Follow the `kata-plan` skill (staged under `.claude/skills/kata-plan/`). Write
an execution-ready plan at `specs/042-todo-filter/plan-a.md`.

Capture HOW and WHEN: ordered steps, each with its intent, the files it
creates/modifies/deletes, the concrete change, and a one-line verification.
Include a one-line `Libraries used:` declaration, a `Risks` section, and an
execution recommendation. Link to the spec and design. Do not restate them.
Write the plan only. Do not implement it.
