This repository is `unitconv`, a small project that already exists. Read
`README.md`, `package.json`, and `src/` to see what it is and who it serves.

Follow the `jidoka-setup` skill. Set up the Jidoka instruction architecture in
this repository.
