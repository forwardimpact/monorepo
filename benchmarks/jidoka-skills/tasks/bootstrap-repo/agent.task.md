This repository is `unitconv`, a small existing project — read `README.md`,
`package.json`, and `src/` to see what it is and who it serves.

Set up the Jidoka instruction architecture in this repository, following the
`jidoka-setup` skill.
