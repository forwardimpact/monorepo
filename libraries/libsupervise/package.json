{
  "name": "@forwardimpact/libsupervise",
  "version": "0.1.29",
  "description": "Process supervision driven by JSON daemon manifests — services stay running and recoverable without manual intervention.",
  "keywords": [
    "supervisor",
    "process",
    "daemon",
    "agent"
  ],
  "homepage": "https://www.forwardimpact.team",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/forwardimpact/monorepo.git",
    "directory": "libraries/libsupervise"
  },
  "license": "Apache-2.0",
  "author": "D. Olsson <hi@senzilla.io>",
  "jobs": [
    {
      "user": "Platform Builders",
      "goal": "Run a Predictable Platform",
      "trigger": "A service fails on a customer machine or an agent session goes sideways — the cause is a missing precondition, an unmanaged process, invisible telemetry, or instructions that drifted from reality.",
      "bigHire": "make platform behavior predictable: preflight checks before anything heavy runs, supervised processes, structured telemetry, and instruction layers that stay honest.",
      "littleHire": "add a daemon to a manifest and trust it restarts on failure.",
      "competesWith": "failing deep in execution instead of at startup; ad-hoc process management; console.log debugging; instruction files nobody validates"
    }
  ],
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./bin/fit-logger.js": "./bin/fit-logger.js",
    "./bin/fit-svscan.js": "./bin/fit-svscan.js"
  },
  "bin": {
    "fit-logger": "./bin/fit-logger.js",
    "fit-svscan": "./bin/fit-svscan.js"
  },
  "files": [
    "src/**/*.js",
    "bin/**/*.js",
    "README.md"
  ],
  "scripts": {
    "test": "bun test test/*.test.js"
  },
  "dependencies": {
    "@forwardimpact/libcli": "^0.1.0",
    "@forwardimpact/libpreflight": "^0.1.0",
    "@forwardimpact/libtelemetry": "^0.1.22",
    "@forwardimpact/libutil": "^0.1.0"
  },
  "devDependencies": {
    "@forwardimpact/libmock": "^0.1.0"
  },
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=22.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
