{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://www.forwardimpact.team/schema/json/defs.schema.json",
  "title": "Engineering Pathway Common Definitions",
  "description": "Shared definitions for all pathway schemas",
  "$defs": {
    "skillProficiency": {
      "type": "string",
      "enum": [
        "awareness",
        "foundational",
        "working",
        "practitioner",
        "expert"
      ],
      "description": "Skill proficiency level from lowest to highest"
    },
    "behaviourMaturity": {
      "type": "string",
      "enum": [
        "emerging",
        "developing",
        "practicing",
        "role-modeling",
        "exemplifying"
      ],
      "description": "Behaviour maturity level from lowest to highest"
    },
    "proficiencyDescriptions": {
      "type": "object",
      "description": "Description of expectations at each skill proficiency",
      "properties": {
        "awareness": { "type": "string" },
        "foundational": { "type": "string" },
        "working": { "type": "string" },
        "practitioner": { "type": "string" },
        "expert": { "type": "string" }
      },
      "required": [
        "awareness",
        "foundational",
        "working",
        "practitioner",
        "expert"
      ],
      "additionalProperties": false
    },
    "maturityDescriptions": {
      "type": "object",
      "description": "Description of expectations at each behaviour maturity level",
      "properties": {
        "emerging": { "type": "string" },
        "developing": { "type": "string" },
        "practicing": { "type": "string" },
        "role-modeling": { "type": "string" },
        "exemplifying": { "type": "string" }
      },
      "required": [
        "emerging",
        "developing",
        "practicing",
        "role-modeling",
        "exemplifying"
      ],
      "additionalProperties": false
    },
    "responsibilitiesByLevel": {
      "type": "object",
      "description": "Responsibility descriptions by skill proficiency",
      "properties": {
        "awareness": { "type": "string" },
        "foundational": { "type": "string" },
        "working": { "type": "string" },
        "practitioner": { "type": "string" },
        "expert": { "type": "string" }
      },
      "required": [
        "awareness",
        "foundational",
        "working",
        "practitioner",
        "expert"
      ],
      "additionalProperties": false
    },
    "skillId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Skill identifier (kebab-case)"
    },
    "behaviourId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Behaviour identifier (kebab-case)"
    },
    "capabilityId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Capability identifier (kebab-case)"
    },
    "levelId": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9]*$",
      "description": "Level identifier (uppercase)"
    },
    "disciplineId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Discipline identifier (kebab-case)"
    },
    "trackId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Track identifier (kebab-case)"
    },
    "modifier": {
      "type": "integer",
      "description": "Modifier value for adjusting levels"
    },
    "normalizedModifier": {
      "type": "integer",
      "minimum": -1,
      "maximum": 1,
      "description": "Modifier value restricted to -1, 0, or 1"
    }
  }
}
